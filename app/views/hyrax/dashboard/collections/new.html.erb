<% provide :page_title, construct_page_title( t('.header', type_title:  hyku_colection_type(@collection.collection_type.title, current_user)) ) %>
<% provide :page_header do %>
  <h1><span class="fa fa-edit" aria-hidden="true"></span> <%= t('.header', type_title: hyku_colection_type(@collection.collection_type.title, current_user)) %> </h1>
<% end %>

<div class="row">
  <div class="col-md-12">
    <%= render 'form' %>
  </div>
</div>
