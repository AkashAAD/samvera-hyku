<% if current_account.persisted? %>
  <% provide :page_title, t("hyrax.pages.tabs.#{@page.name}") %>
  <%= displayable_content_block @page, class: 'row', id: 'content_block_page' %>
<%else%>
  <%= render 'ebsco_' + params[:key] %>
<%end%>